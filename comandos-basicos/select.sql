
-- Comando select
select * from sales.customers -- busca por todas as colunas da tabela customers
select email from sales.customers -- busca por todos os emais da tabela customers
select first_name from sales.customers -- busca por todos os nomes na tabela de customers
select brand from sales.products -- busca por todas as marcas na tabela products
select model from sales.products -- busca por todos os modelos na tabela products
select model, brand from sales.products -- busca por todos os modelos e todas as marcas na tabela products
